doctype
html(lang=ru)
	head
		include includes/head_common.pug
		script(src="/js/faculty.js")
		style
			|	a.load-nav.current {
			|		background-color: #dedede;
			|	}
			|	a.load-more {
			|		display: none;
			|	}
	body
		include includes/header.pug
		div.wrp
			div.respond-line
				a.load-nav.current(href="#", type="university") ВУЗ 
					span(id="countnew-university")
				a.load-nav(href="#", type="faculty") Факультет 
					span(id="countnew-faculty")
				a.load-nav(href="#", type="department") Направление
					span(id="countnew-department")
				a.load-nav(href="#", type="studyyear") Курс
					span(id="countnew-studyyear")
			div.last-five
				h3 Если Вы не знаете человека, который указал вашу СПЕЦИАЛЬНОСТЬ или КУРС, то отправьте жалобу с его профиля. Так мы вместе сможем бороться с мошенниками, риелторами и фейковыми пользователями.
				div(id="content")
				a#load-more.load-more(href="#") Загрузить больше...
		include includes/footer.pug